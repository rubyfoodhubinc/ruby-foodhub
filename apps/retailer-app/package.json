{
  "name": "rubyfoodhub-wholesale-app",
  "private": true,
  "version": "1.0.0",
  "description": "Ruby FoodHub Wholesale (retailer portal) — native app wrapper (Capacitor)",
  "scripts": {
    "sync-www": "node sync-www.js",
    "sync": "node sync-www.js && npx cap sync",
    "open:android": "npx cap open android",
    "open:ios": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0",
    "@capacitor/ios": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
